#include <stdlib.h>
#include <stdio.h>

int main(int argc,char** argv)
{
	int N=12;
	int* max=malloc(sizeof(int)*N);
    *max=1;

    int* num=malloc(sizeof(int)*N);
    int max_index=N-1;
    int min_index=N-1;

    while ( *num!=1 )
    {
    	
    }
}